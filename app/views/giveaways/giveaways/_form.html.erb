<%= simple_form_for :giveaways, @giveaway do |f| %>
	
	<%= f.input :title %>

	<%= f.input :description %>

	<%= f.input :thank_you_message, :input_html { value: t('giveaways.giveaways.forms.default_thank_you_message') } %>

	<%= f.input :starts_at %>

	<%= f.input :ends_at %>

	<%= f.button @giveaway.persisted? ? t('giveaways.giveaways.forms.update') : t('giveaways.giveaways.forms.create') %>
<% end %>